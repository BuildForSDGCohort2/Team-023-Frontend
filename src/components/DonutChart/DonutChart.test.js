import React from "react";
import { render } from "@testing-library/react";
import DonutChart from "./DonutChart";

describe("DonutChart", () => {
  test("renders DonutChart component", () => {
    render(<DonutChart />);
  });
});
